<form class="float-right mb-1 mt-1 d-inline" method="post" action="{{ path('etudiant_delete', {'id': etudiant.id}) }}" onsubmit="return confirm('Etes-vous sûr?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ etudiant.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
