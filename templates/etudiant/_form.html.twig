{{ form_start(form, {'attr':{'class': 'needs-validation', 'novalidate': 'true'}}) }}
    <div class="form-group">
        <label for="{{ form.prenom.vars.id }}">{{ form.prenom.vars.label }}</label>
        {{ form_widget(form.prenom, {'attr': {'class': 'form-control'}}) }}

        <small class="form-text text-muted">{{ form_help(form.prenom) }}</small>
        <div class="invalid-feedback">{{ form_errors(form.prenom) }}</div>
  </div>

    <div class="form-group">
        <label for="{{ form.nom.vars.id }}">{{ form.nom.vars.label }}</label>
        {{ form_widget(form.nom, {'attr': {'class': 'form-control'}}) }}

        <small class="form-text text-muted">{{ form_help(form.nom) }}</small>
        <div class="invalid-feedback">{{ form_errors(form.nom) }}</div>
  </div>

      <div class="form-group">
        <label for="{{ form.groupe.vars.id }}">{{ form.groupe.vars.label }}</label>
        {{ form_widget(form.groupe, {'attr': {'class': 'form-control'}}) }}

        <small class="form-text text-muted">{{ form_help(form.groupe) }}</small>
        <div class="invalid-feedback">{{ form_errors(form.groupe) }}</div>
  </div>
    <button class="btn btn-success">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}

<script>
// Example starter JavaScript for disabling form submissions if there are invalid fields
(function() {
  'use strict';
  window.addEventListener('load', function() {
    // Fetch all the forms we want to apply custom Bootstrap validation styles to
    var forms = document.getElementsByClassName('needs-validation');
    // Loop over them and prevent submission
    var validation = Array.prototype.filter.call(forms, function(form) {
      form.addEventListener('submit', function(event) {
        if (form.checkValidity() === false) {
          event.preventDefault();
          event.stopPropagation();
        }
        form.classList.add('was-validated');
      }, false);
    });
  }, false);
})();
</script>